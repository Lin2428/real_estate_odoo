<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <!-- Attachment -->
        <record id="view_attachment_form" model="ir.ui.view">
            <field name="model">ir.attachment</field>
            <field name="arch" type="xml">
                <form string="Add Documents">
                   <sheet>
                    <group>
                        <group class="w-100">
                            <field name="type" invisible="True"/>
                            <label for="datas" string="Select a file" invisible="type == 'url'"/>
                            <field name="datas" nolabel="1" class="w-100" filename="name" invisible="type == 'url'"/>
                            <field name="name" string="Document name" invisible="datas==False"/>
                            <field name="mimetype" invisible="True"/>
                        </group>
                        <group string="Attached To" invisible="True">
                            <field name="res_model"/>
                            <field name="res_field"/>
                            <field name="res_id"/>
                            <field name="res_name"/>
                            <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                            <field name="public"/>
                        </group>

                    </group>
                  </sheet>
                </form>
            </field>
        </record>

</odoo>
